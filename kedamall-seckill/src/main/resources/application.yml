feign:
  client:
    config:
      default:
        connect-timeout: 20000
        read-timeout: 20000
spring:
  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
